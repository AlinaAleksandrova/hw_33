'use strict';

const addFunction = function(a, b) {
    return a + b;
}

const subtFunction = function(a, b) {
    return a - b;
}

const multFunction = function(a, b) {
    return a * b;
}

console.log(addFunction(2, 2));
console.log(subtFunction(2, 2));
console.log(multFunction(2, 2));